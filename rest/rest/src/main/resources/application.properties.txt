# Dica importante: as propriedades do Spring seguem o padrão chave=valor

spring.application.name=rest

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/rest_db?createDatabaseIfNotExist=true&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=user
spring.datasource.password=kwd2%/O.?NSN4ZwWXJ[Z

# ddl-auto: update  # ← ANTIGO: Hibernate criava/alterava tabelas automaticamente (perigoso em produção!)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# Flyway: Sistema de Migrações do Banco de Dados
# Scripts SQL ficam em src/main/resources/db/migration/ com formato V{numero}__{descricao}.sql
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

# Chave secreta em Base64 (256-bit / 32 bytes) para JWT
security.jwt.secret-key=YWJjZGVmZ2hpamtsbW5vcHFyc3R1dnd4eXoxMjM0NTY3ODkwYWJjZGVmZ2hpams=
security.jwt.expiration=36000000

springdoc.swagger-ui.path=/swagger-ui.html
# springdoc.swagger-ui.enabled=false
springdoc.api-docs.path=/v1/api-docs
# springdoc.api-docs.enabled=false

# Níveis de Logs
# logging.level.root=TRACE
logging.level.com.example.rest=DEBUG

server.port=8090

# Prefixo global para todas as suas URLs (ex: http://localhost:8080/api/v1/users)
# server.servlet.context-path=/api/v1

# Configurações adicionais do Tomcat se necessário
# server.tomcat.max-connections=8192
# server.tomcat.threads.max=200
# server.tomcat.threads.min-spare=10
# server.tomcat.connection-timeout=20000

app.admin.email=admin@rest.com
app.admin.password=admin123
